import React from 'react';
import PropTypes from 'prop-types';
const propTypes = {};
const defaultProps = {};
const PasteUrls = ({}) => (
  <div className="cell">
    <button className="button expanded">Paste custom urls</button>
  </div>
);
PasteUrls.propTypes = propTypes;
PasteUrls.defaultProps = defaultProps;
export default PasteUrls;
