import React from 'react';
import PropTypes from 'prop-types';
const propTypes = {};
const defaultProps = {};
const Upload = ({}) => (
  <div className="cell">
    <button className="button expanded">Upload a spreadsheet</button>
  </div>
);
Upload.propTypes = propTypes;
Upload.defaultProps = defaultProps;
export default Upload;
