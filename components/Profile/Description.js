import React from 'react';
import PropTypes from 'prop-types';
const propTypes = {};
const defaultProps = {};
const Description = ({}) => (
  <p className="h4 text-center text--profile">
    TADA here is the BookiWooki profile.You can add and edit the info that we
    have captured for you.
  </p>
);
Description.propTypes = propTypes;
Description.defaultProps = defaultProps;
export default Description;
